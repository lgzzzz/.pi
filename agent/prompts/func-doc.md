---
description: 分析函数返回值及其关联关系，生成详细文档
argument-hint: "<函数名> <文件路径>"
---
分析函数 `$1`（位于 `$2`），提供一份详细文档，内容包括：

1. **函数概述**：简要说明函数的功能和用途。
2. **返回值详解**：
   - 返回值的类型和结构（如基本类型、对象、数组等）。
   - 每个返回字段的含义、取值范围和可能的边界情况。
   - 不同执行路径下返回值的变化情况。
3. **返回值关联关系分析**：
   - 各返回值字段之间的依赖关系和约束。
   - 返回值与输入参数之间的映射关系。
   - 是否存在互斥或组合的返回模式。
4. **使用示例**（如有必要）：给出典型的调用和返回值示例。

请直接阅读 `$2` 中的源代码进行分析，不要猜测。
